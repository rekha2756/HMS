// config/database.js
module.exports = {
  // This file is used by dbService (which reads env directly).
  // However, we keep it for central configuration if needed.
  // The main connection parameters are now in .env.
};